# --- Dependances de production ---
# Versions figees : le modele est un objet serialise par scikit-learn, une version
# differente au chargement peut casser la deserialisation ou changer les predictions.
scikit-learn==1.6.1
pandas==2.3.3
numpy==2.0.2
joblib==1.4.2

# API
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1

# Base de donnees : SQLAlchemy sert d'ORM, psycopg est le pilote PostgreSQL.
# psycopg[binary] embarque libpq, ce qui evite d'installer PostgreSQL sur la machine.
SQLAlchemy==2.0.36
psycopg[binary]==3.2.3

# Authentification : PyJWT signe les jetons, passlib+bcrypt hache les mots de passe.
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
python-multipart==0.0.20
